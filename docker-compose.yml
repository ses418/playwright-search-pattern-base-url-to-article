version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: playwright-search-service
    ports:
      - "5070:5070"
    env_file:
      - .env
    # Increase shared memory to prevent browser crashes
    shm_size: "2gb"
    # Resource limits - CRITICAL for production stability
    deploy:
      resources:
        limits:
          cpus: "2"
          memory: 4G
        reservations:
          cpus: "1"
          memory: 2G
    # Restart policy - use unless-stopped to prevent crash loops
    restart: unless-stopped
    # Health check
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5070/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
